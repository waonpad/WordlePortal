export type WordleCommentData = {
    wordle_id: number;
    comment: string;
    submit: string;
}

export type WordleCommentErrorData = {
    wordle_id: string;
    comment: string;
    submit: string;
}